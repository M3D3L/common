import { footerData } from "./components/footer";
import { highlightsData } from "./components/hightlights";
import { heroData } from "./components/hero";
import { skillsData } from "./components/skills";

export const layoutConfig = {
    heroData,
    skillsData,
    highlightsData,
    footerData
}